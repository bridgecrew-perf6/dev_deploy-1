<?phpnamespace Nemundo\App\Linux\Cmd;use Nemundo\Core\Command\AbstractCommand;use Nemundo\Core\Local\AbstractLocalCommand;class ChmodCmd extends AbstractCmd  // AbstractLocalCommand{    public $path;    public function getCmd()    {        $this->addCommandLine('chmod -R 777 '.$this->path);        return parent::getCmd(); // TODO: Change the autogenerated stub    }    /*        public function makeExecutable($filename)        {            $this->addCommand('chmod +x ' . $filename);            $this->runCommand();        }*/}