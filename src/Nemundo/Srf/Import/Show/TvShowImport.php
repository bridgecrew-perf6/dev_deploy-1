<?phpnamespace Nemundo\Srf\Import\Show;use Nemundo\Srf\Config\BusinessUnit;use Nemundo\Srf\Data\Show\Show;use Nemundo\Srf\MediaType\TvMediaType;use Nemundo\Srf\Reader\Show\TvShowJsonReader;class TvShowImport extends AbstractShowImport{    public function importData()    {        $reader = new TvShowJsonReader();        $reader->businessUnit = BusinessUnit::SRF;        foreach ($reader->getData() as $item) {            //if (!$this->existsItem($item->srfId)) {                $data = new Show();                $data->updateOnDuplicate = true;                $data->mediaTypeId = (new TvMediaType())->id;                $data->show = $item->show;                $data->description = $item->description;                $data->srfId = $item->srfId;                $data->image->fromUrl($item->imageUrl);                $data->crawler = false;                $data->save();            //}            /*            $id=new ShowId();            $id->filter->andEqual($id->model->srfId, $item->srfId);            $dataId=$id->getId();*/            /*$type = new TvShowContentType($dataId);            (new IndexBuilder())->buildIndex($type);*/            /*            $type = new TvShowContentType();            $type->show = $item->show;            $type->srfId = $item->id;            $type->saveType();*/        }    }}